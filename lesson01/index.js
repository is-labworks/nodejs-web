const solveLinearEquation = require('./solveLinearEquation');
const solveQuadraticEquation = require('./solveQuadraticEquation');

// Example usage:
console.log('Linear Equation (2x + 4 = 0):', solveLinearEquation(2, 4));
console.log('Quadratic Equation (1x^2 + -3x + 2 = 0):', solveQuadraticEquation(1, -3, 2));